import React from 'react';

import { Container, TrincaIcon } from './styles';

const Footer: React.FC = () => {
  return (
    <Container>
      <TrincaIcon />
    </Container>
  );
};

export default Footer;
